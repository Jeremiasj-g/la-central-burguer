import type { SaleLedgerEntry } from '../types/venta.types';
import { formatCurrency, formatDateTime } from '@/shared/utils/format.utils';
export function VentasTable({ sales }: { sales: SaleLedgerEntry[] }) { return <div className="overflow-x-auto rounded-sm border border-neutral-200 bg-white shadow-sm custom-scrollbar"><table className="w-full min-w-[850px] text-left"><thead className="bg-neutral-50 text-xs font-black uppercase tracking-[.18em] text-neutral-500"><tr><th className="p-4">Pedido</th><th className="p-4">Cliente</th><th className="p-4">Entrega</th><th className="p-4">Pago</th><th className="p-4">Fecha</th><th className="p-4 text-right">Subtotal</th><th className="p-4 text-right">Envío</th><th className="p-4 text-right">Total</th></tr></thead><tbody>{sales.map((sale)=><tr key={sale.id} className="border-t border-neutral-100 text-sm"><td className="p-4 font-mono font-black text-central-orange">{sale.orderCode}</td><td className="p-4"><strong>{sale.customerName}</strong><div className="text-xs text-neutral-500">{sale.customerPhone}</div></td><td className="p-4">{sale.deliveryMethod==='delivery'?'Delivery':'Retiro local'}</td><td className="p-4 capitalize">{sale.paymentMethod}</td><td className="p-4 text-neutral-600">{formatDateTime(sale.createdAt)}</td><td className="p-4 text-right">{formatCurrency(sale.subtotal)}</td><td className="p-4 text-right">{formatCurrency(sale.deliveryCost)}</td><td className="p-4 text-right font-black">{formatCurrency(sale.total)}</td></tr>)}</tbody></table></div>; }
